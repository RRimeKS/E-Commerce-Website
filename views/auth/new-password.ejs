<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head') %>
    <style>
        .signin-wrapper {
            display: flex;
            align-items: center;
            justify-content: center;
            height: 80vh;
        }
    </style>

    <body>

        <div class="container">
            <%- include('../partials/navbar') %>


                <% if (typeof message !="undefined" ) { %>
                    <div class="alert-wrapper mt-5 d-flex justify-content-center align-items-center w-100">
                        <div class="alert alert-<%= message.class %>">
                            <%= message.text %>
                        </div>
                    </div>
                    <% } %>

                        <form method="post">
                            <div class="signin-wrapper">
                                <div class="form-signin">
                                    <h1 class="h4 mb-3 fw-normal">Petexpress.com</h1>
                                    <div class="form-floating ">
                                        <input type="text" name="password" class="form-control">
                                        <label for="floatingInput">Yeni şifre</label>
                                    </div>
                                    <div class="form-floating mt-5">
                                        <input type="submit" class="btn btn-primary" value="Şifremi güncelle">
                                    </div>

                                </div>
                            </div>
                        </form>
        </div>

    </body>

</html>